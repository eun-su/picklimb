rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 익명 로그인 사용자를 포함해 Firebase Authentication 사용자를 제한합니다.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
